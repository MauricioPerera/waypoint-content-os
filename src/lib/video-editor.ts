import { FFmpeg } from '@ffmpeg/ffmpeg'; import { fetchFile } from '@ffmpeg/util';
type Clip={file:File;trimStart:number;trimEnd:number};let engine:FFmpeg|null=null;
export async function renderProject(clips:Clip[],format:string,fit:'fill'|'fit'){if(!engine){engine=new FFmpeg();await engine.load()}for(let i=0;i<clips.length;i++)await engine.writeFile(`input${i}.mp4`,await fetchFile(clips[i].file));const size=format==='9:16'?'1080:1920':format==='1:1'?'1080:1080':format==='4:5'?'1080:1350':'1920:1080';const vf=fit==='fit'?`scale=${size}:force_original_aspect_ratio=decrease,pad=${size}:(ow-iw)/2:(oh-ih)/2`:`scale=${size}:force_original_aspect_ratio=increase,crop=${size}`;await engine.exec(['-i','input0.mp4','-vf',vf,'-c:v','libx264','-preset','ultrafast','-movflags','+faststart','output.mp4']);const data=await engine.readFile('output.mp4');return new Blob([data as BlobPart],{type:'video/mp4'})}
