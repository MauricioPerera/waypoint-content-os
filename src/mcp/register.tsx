'use client';import {useEffect,useRef} from 'react';import {registerTools} from '@nekuda/webmcp-sdk';import {createTools,type State} from './tools';export function WebmcpRegistrar({state}:{state:State}){const current=useRef(state);Object.assign(current.current,state);useEffect(()=>{const tools=createTools(current.current);const unique=tools.filter((tool,index,list)=>index===list.findIndex(candidate=>candidate.name===tool.name));const registration=registerTools(unique);return()=>registration.unregister()},[]);return null}
