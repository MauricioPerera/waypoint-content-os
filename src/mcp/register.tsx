'use client';import {useEffect} from 'react';import {registerTools} from '@nekuda/webmcp-sdk';import {createTools,type State} from './tools';export function WebmcpRegistrar({state}:{state:State}){useEffect(()=>{const r=registerTools(createTools(state));return()=>r.unregister()},[state]);return null}
